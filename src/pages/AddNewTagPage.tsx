import React from "react";
import { Space, Typography } from "antd";

const { Title } = Typography;

function AddNewTagPage() {
  return (
    <Space direction="vertical">
      <Title level={5}>Add New Tag</Title>
    </Space>
  );
}

export default AddNewTagPage;
